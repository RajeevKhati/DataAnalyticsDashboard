import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: { main: "#47b747", dark: "#02BB7D" },
    background: {
      default: "#ffffff",
      paper: "#f6f7f9",
    },
  },
});
