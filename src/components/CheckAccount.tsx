import { Paper } from "@mui/material";

export default function CheckAccount() {
  return (
    <Paper
      elevation={2}
      sx={{ height: "100%", width: "100%", bgColor: "blue" }}
    >
      CheckAccount
    </Paper>
  );
}
